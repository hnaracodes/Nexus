# fly.toml app configuration file generated for nexus-mvp on 2026-07-29T18:14:11-07:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'nexus-mvp'
primary_region = 'sjc'

[build]
  dockerfile = 'Dockerfile'

[env]
  NEXUS_DATA_DIR = '/data'
  PORT = '8080'
  # On the persistent volume, NOT ephemeral container storage. A room's clone
  # has to outlive a restart or every recovered room would have an empty
  # working directory under a link that still resolves. Sized with the volume:
  # private clones accumulate here and nothing evicts them yet.
  NEXUS_WORKDIR = '/data/work'

[[mounts]]
  source = 'nexus_data'
  destination = '/data'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type = 'connections'
    hard_limit = 200
    soft_limit = 150

[[vm]]
  size = 'shared-cpu-1x'
  memory = '1gb'
  cpus = 1
  memory_mb = 1024
